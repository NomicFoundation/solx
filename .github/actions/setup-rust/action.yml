name: Setup Rust
description: Sets up Rust toolchain declaratively via rust-toolchain.toml
inputs:
  toolchain:
    description: Rust toolchain to install (empty = use rust-toolchain.toml)
    required: false
    default: ''
  target:
    description: Target triple to install for this toolchain
    required: false
    default: ''
  components:
    description: Comma-separated components to install
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Setup Rust toolchain
      uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17
      with:
        toolchain: ${{ inputs.toolchain }}
        target: ${{ inputs.target }}
        components: ${{ inputs.components }}
        cache: false
        rustflags: ""
        override: false
